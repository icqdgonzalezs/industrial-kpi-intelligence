services:
  - type: web
    name: industrial-kpi-intelligence
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn dashboard.dash_app:server
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
