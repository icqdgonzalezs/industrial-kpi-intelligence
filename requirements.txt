# ============================================================
# Industrial KPI Intelligence — dependencias del proyecto
# ============================================================
# Incluye ambos stacks (Dash legacy + FastAPI nuevo) y el
# tooling necesario para CI (pytest, ruff).
# ============================================================

# --- Stack FastAPI (app/) ---
fastapi==0.141.1
uvicorn==0.53.0
sqlmodel==0.0.46
SQLAlchemy==2.0.54
Jinja2==3.1.6

# --- Stack Dash legacy (dashboard/ + src/) ---
dash==4.4.1
plotly>=5.0,<6
pandas>=2.1,<3
numpy>=1.26,<2
python-dateutil==2.9.0.post0
PyYAML>=6.0,<7

# --- Testing + lint (CI) ---
pytest==9.1.1
pytest-cov==7.1.0
ruff==0.16.6

# --- Core (común) ---
pydantic==2.13.5
